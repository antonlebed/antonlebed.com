/* page_crt_stats.ax -- EMPTY. All CRT stats code moved to router.ax (S1210). */
/* Compiler position-dependent corruption made functions in this file
   produce corrupted bytecode. Moving to router.ax dodges the bad region. */
